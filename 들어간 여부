int x=480;
int y=480;
int z=40;

void setup() {
  size(1000, 1000);
}
void draw() {
  background(204);
  rect(x, y, z, z);
  if ((mouseX>x)&&(mouseX<x+z)&&(mouseY>y)&&(mouseY<y+z)) {
    fill(0, 0, 255);
  } else {
    fill(255, 0, 0);
  }
}
