float angle = 0.0;
float angleDirection=1;
float speed=0.005;

void setup(){
  size(1000,1000);
  rectMode(CENTER);
}

void draw(){
    background(255,0,0);
    translate(mouseX,mouseY);
    float scalar=mouseX/60.0;
    scale(scalar);
    strokeWeight(1.0/scalar);
    rect(0,0,30,30);
}
